<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Uygulamanın temel teması -->
    <style name="Theme.MyApplicationennew" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Ana renkler -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/buttonText</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:windowBackground">@color/backgroundLight</item>
    </style>

    <!-- (isteğe bağlı) Base tema bırakılabilir -->
    <style name="Base.Theme.MyApplicationennew" parent="Theme.MyApplicationennew" />

</resources>
